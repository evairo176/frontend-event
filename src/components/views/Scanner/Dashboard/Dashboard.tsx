import React from "react";
import useDashboard from "./useDashboard";

type Props = {};

const Dashboard = (props: Props) => {
  return <div className="min-h-screen">ini dashboard</div>;
};

export default Dashboard;
